import { SectionTitle } from "@/components/ui/section-title";
import { LuBox } from "react-icons/lu";

const ProjectsTitle = () => {
  return <SectionTitle title="Projects" icon={LuBox} />;
};

export default ProjectsTitle;
