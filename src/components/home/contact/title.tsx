import { SectionTitle } from "@/components/ui/section-title";
import { LuMail } from "react-icons/lu";

const ContactTitle = () => {
  return <SectionTitle title="Get In Touch" icon={LuMail} />;
};

export default ContactTitle;
